# commit-validator

_Docker container for module /willsoto/validate-commit_

## Getting Started

To install:

    npm i --save commit-validator

In your project:

``` javascript
 // => Example
```

## npm scripts

   * `tes`
   * `test:unit`
   * `test:int`
   * `cove`
   * `cover:unit`
   * `lint`
   * `serve:coverage`
   * `serve:linter`
